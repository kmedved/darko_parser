#Black 24.x+ is required to drop the 'typed-ast' dependency which fails in 3.12

black>=24.2.0
codecov>=2.1.13

#Use editable install for the local package

-e .

#'neovim' package is deprecated alias; use pynvim

pynvim>=0.5.0
pipdeptree>=2.13.0

#Psycopg2 2.9.9+ is required for Python 3.12 binary wheels

psycopg2-binary>=2.9.9
pytest-cov>=4.1.0
setuptools>=69.0.0

#'sklearn' package is deprecated; use scikit-learn

scikit-learn>=1.4.0

#SQLAlchemy 1.3 is very old; 2.0 is current, but 1.4 is a safer bridge

SQLAlchemy>=1.4.50
wheel>=0.42.0